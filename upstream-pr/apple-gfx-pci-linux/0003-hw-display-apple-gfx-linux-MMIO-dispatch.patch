From: Matthew Jackson <matthew@pq.io>
Subject: [PATCH 3/8] hw/display/apple-gfx-linux: MMIO dispatch

Wire the PCI BAR0 MMIO region to `lagfx_mmio_read` /
`lagfx_mmio_write` in libapplegfx-vulkan. The library is the
authoritative decoder for the register layout - it mirrors
Apple's ParavirtualizedGraphics.framework's internal
dispatch and returns a single 32-bit value per read.

Trace events `apple_gfx_read` / `apple_gfx_write` are
already defined by the upstream apple-gfx.m sibling (shared
`hw/display/trace-events`). The Linux port reuses those
without adding new entries.

MemoryRegionOps is configured with 4-byte impl and 8-byte
valid ranges, matching the macOS sibling.

Signed-off-by: Matthew Jackson <matthew@pq.io>
---
 hw/display/apple-gfx-common-linux.c | ~40 insertions(+)
 1 file changed, ~40 insertions(+)
